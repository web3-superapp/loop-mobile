# LOOP current product decisions

> Canonical current scope. Updated 2026-09-02.

This document applies to the Flutter source at the repository root. Material under `reference/legacy-prototype/` is frozen history and does not override these decisions.

## Product shape

- LOOP is a Flutter iOS and Android app. Web output is only a local UI-verification target.
- Decision 0048 adopts five primary destinations in this order: Community,
  Mining, Launch, Market and Wallet. Community is the post-login home; Chat is
  a Community child flow, while Profile is opened as a child surface rather
  than a bottom-navigation destination.
- `/home` and `/launchpad` are compatibility redirects to `/community` and
  `/launch`. Decision 0050 makes `docs/product/routes-manifest.json` (93
  routes, mirrored by `lib/core/navigation/route_manifest.dart`) the only
  route inventory; every unimplemented slug mounts a truthful pending surface
  and every retired location is logged and returned to Community. The former
  103-surface catalog is read-only history, not a routing source or evidence
  that the 93 pages are complete.
- LOOP is spot-only. Perpetuals are disabled, have no primary-feature entry, and receive no further product work.
- Privy is the selected login, wallet and final authorization boundary.
- Reown AppKit 1.8.4 is accepted only as the connection and `personal_sign` transport for external EVM credentials under Privy. It is not a second identity system or a transaction wallet.
- The retained Hyperliquid Core perpetual implementation is disabled history and must not be relabelled as spot. Decision 0017 accepts a separate public Hyperliquid Testnet Spot market-discovery contract, and decision 0019 adds bounded public candles only after that discovery source admits the exact provider market. Spot execution remains unavailable and requires its own backend-mediated decision.

## Pay priority and delivery

Product priority and current delivery are separate:

- Product priority uses A, B and C. It is not a release phase or delivery promise.
- The retired catalog recorded 47 A-priority, 46 B-priority and 10 C-priority surfaces; the priorities below still describe Pay.
- B5 is A priority, B6 and B7 are B priority, and B8 is C priority.
- B5 through B8 are all deferred from the current release.
- Wallet keeps one Pay card labelled unavailable so the planned V2 product position remains visible after Home is retired.
- Every Pay route is informational only. It must not open a scanner, request camera access, collect an amount or recipient, select a payment provider, or submit a transaction.

## Launchpad delivery

- Launch remains the third first-class destination at `/launch`. All eleven
  Launch pages and all six Mining pages are mounted against the V2 launch,
  mining and referral modules (decision 0058); the non-actionable placeholder
  they replaced is retired with decision 0027.
- The mounted surface is an **off-chain catalogue and application flow**. The
  Launch contract baseline and the mining formula version are both undelivered,
  so every on-chain and formula-derived figure — the four state axes, price,
  caps, fees, supply, holders, graduation progress, power, output, rewards,
  rank and referral boost — renders the em dash with the server's own
  `reasonCode`, never `0` and never a fixture. Configuration slots without a
  confirmed version render "待确认（configVersion）".
- Catalogue segments come only from `scheduleStatus`, and `awaitingSchedule` is
  its own segment. "已毕业" is a liquidity-axis fact and stays unavailable; an
  ended schedule never implies graduation.
- `launch-apply` is a real owner-scoped resource: draft, compare-and-set edit,
  submit, and the server's six review states, with a returned application
  editable and re-submittable. Attachments and KYB have no provider and expose
  no upload control.
- `launch-trade` keeps its form visible and its main action disabled on the
  server's permanent `503`; there is no sell side before graduation.
  `loop-stake` is non-executable as a whole page, and Launch eligibility does
  not depend on staking. `mining-rewards` disables its claim on the server's
  `claimExecutable: false`. No Launch or Mining path constructs a transaction
  or opens the signing sheet.
- `referral` reads `GET /v2/referral`: one invite code per account, per-level
  counts grouped by the server's `validationStatus`, and an unavailable boost.
  Only `valid` edges count as effective relationships. Every figure is a Mining
  Power input — never a commission, a payout or a downline income.

## Communication

- Stream Chat + Stream Video/Audio Rooms is the selected communication integration.
- The production Chat inbox uses Stream's official client, persistence, token provider, channel-list controller/view, channel scope and message page. Stream owns messages, pagination, ACK/read state, presence, typing and offline synchronization.
- `communicationGatewayProvider` remains the fail-closed seam for preview-only secondary communication surfaces that have not yet moved to official SDK UI. Only `lib/main_preview.dart` injects memory data; `lib/main.dart` never injects it.
- Communication mode is explicit: memory data is `preview`; the Stream seam is `production`. Preview UI continuously identifies itself as offline, simulated and not connected.
- The official client may be constructed from the public API key without connecting. A production session source must obtain a backend-derived Stream user ID and short-lived server-issued token; missing or failed authorization prevents controller/message UI from mounting.
- Privy user ID is only an opaque logout/account-switch key in Flutter. It is never converted into the Stream user ID. Each principal gets an isolated Stream client/persistence pair; logout and account switches retire the old pair, invalidate in-flight authorization, and disconnect without deleting per-user offline history.
- Text chat composition is wired through official UI. Attachments and voice recording remain disabled until platform permissions, upload policy and provider/device verification are complete.
- Incoming Stream `token_card.v1` attachments are the narrow exception to disabled attachment composition: official Stream message rendering may display their strict identifier-only reference. A compliant producer/server contract forbids persisting price, liquidity, risk, ownership, provider links, watch state, or trading actions; the receiver hides violations without claiming they were never stored.
- The Token Card renderer is synchronous and fail-closed. Missing, extra, malformed, repeated, mixed, or unknown-version payloads show no supplied facts. Valid cards remain `Current facts unavailable` until a separately freshness-bounded backend facts projection exists; preview facts stay visibly marked `开发预览` and cannot mutate a watchlist or trade.
- Raw Token Cards are intercepted before Stream's computed link/media type can select a default renderer, and standard top-level URL/action/display fields are rejected. Compact message and draft previews replace every raw Token Card with a fixed safe label so captions, titles, URLs, and mutable facts cannot escape through channel, reply, thread, or search previews. Outgoing Token Card composition remains disabled until a backend or Stream before-send policy enforces the same complete wire shape; Flutter receive validation alone cannot guarantee stored data.
- The legacy group, direct-message, group-information, request, search, and token/facts preview surfaces remain Preview-only. Step 4 gave the four conversation slugs their V2 production pages, so the route picks the labelled fixture page only in Preview mode; production navigation can never mount a fixture conversation.
- The legacy E9 asset-message route remains Preview-only but is now Spot-only: every fixture fact is visibly labelled, no position/trade/copy/save/alert capability is implied, its sole enabled action opens `/market`, and Watch stays disabled. Generic internal names such as `assetSnapshot` remain implementation labels and do not authorize Perp semantics.
- Legacy Message Requests remain process-local Development Preview state. Accept removes one pending fixture without creating or opening a Stream conversation, Ignore causes no sender interaction, and Report removes the fixture without submitting moderation. Unknown or already resolved IDs fail, pending actions are single-flight per card, and the Inbox count derives from the current Preview projection rather than a fixed badge.
- Legacy Preview message conversations now use one exact registered `conversationId` across Inbox navigation, group/direct pages, group information, and scoped search. Missing, repeated, unknown, or ID/kind-mismatched targets show an unavailable surface and the memory gateway never falls back to another fixture. Production remains on Stream's official full-CID route; Audio Room target selection remains backend-owned.
- Legacy Preview group-information switches change only labelled process-local layout state and never claim Stream notification persistence. Turning off the parent example clears its mentions-only dependent state. Member operations and Leave remain disabled until an official Stream mutation is connected; closing a dialog is not a group-membership change.
- Chat exposes one fixed create menu and Profile exposes `我的好友`; production now uses the reviewed principal-bound LOOP social adapter for relationship-aware Alias-prefix search, accepted friends, incoming/outgoing requests, request decisions and backend-created group/direct channels. `public_profile_id` is the only command target, `profile_code` is the immutable display discriminator, and mutable/non-unique account Alias rows remain valid. Wallet addresses, Privy/LOOP IDs, Stream user IDs and group aliases never become public-search or client-selected membership identifiers.
- Friend/request decisions remain server facts. Each mutation uses a new UUIDv4 as both `Idempotency-Key` and `operation_id`; an ambiguous or malformed-success POST is queried before the exact request can be replayed, while anything except the exact route-specific operation-not-found response never authorizes a duplicate. Group/direct `202` operations poll at the greater advertised delay under monotonic time and attempt ceilings, and `operator_required` stops rather than creating a second channel. UUID/body retention is currently process-local; process-death recovery needs a separately reviewed encrypted journal or backend owner-scoped discovery contract.
- Groups submit 2–29 accepted friends' `public_profile_id` values and accept only a matching `group_id`, unordered friend set and canonical `messaging:loop_group_*` CID; direct creation converges on the backend's fixed `messaging:loop_direct_*` CID. The returned CID still passes the exact online Stream CID + current-member gate before official channel UI mounts. Stream remains the truth source for messages and realtime channel state; Flutter never creates channels, chooses Stream identities or mutates membership directly. The labelled memory Preview still returns no Stream CID.
- Social Privacy is separate from public Profile Privacy and controls only new friend requests, group invites and direct messages with fail-closed defaults. A group Alias is an immutable `group_id`-scoped pseudonym; direct channels have no such namespace, peer search returns only `group_alias_id` plus Alias, and LOOP makes no strong-anonymity claim. Group Stream presentation uses only the exact current-member v1 projection and otherwise the neutral `群成员` label across messages, references, mentions, reactions, threads, typing, list previews and avatars; it never falls back to an account-level name or stable Stream ID. New groups use the receipt's `group_id`; after restart an already membership-gated Stream group may resolve its validated channel ID through the backend without an idempotency key, while known direct IDs fail locally. Friend deletion, block/unblock, QR discovery, wallet-address search and group-member administration remain out of this slice.
- Stream Video now has a foreground-only, principal-bound SDK lifecycle and a narrow backend identity/token seam. The SDK is constructed only after an initial backend token is available, connects with push registration disabled, and is retired on route disposal, logout, or account change.
- Production voice surfaces never render preview room/member data. Audio Room now has a foreground lobby, backend-authorized target seam, muted single-flight join, official `CallState` participant/capability/media UI, explicit Speak action and principal/target/client cleanup. Missing Video tokens or room locator keep the join action disabled.
- Decision 0055 makes every Audio Room a community resource: the lobby is opened from a community record, not from the generic Chat inbox, because a room without a community has no locator, and the authorized room is handed to the reviewed lobby as a constructor argument rather than through a scoped provider. While the `voiceRooms` capability's provider-evidence flag is pending, `voiceroom` and `voiceroom-full` render an explanation and issue no request at all. The room resource carries no speaker directory, so removing a speaker is unavailable; a hand raise is a request to speak and never a removal target.
- A chat notification that names a channel LOOP cannot map produces no navigation intent at all, and the `/chat/channel/:cid` deep link records the unmatched location before returning to Community. A terminal `operatorRequired` outcome is shown as an unresolved result with no retry: retrying would open a second operation.
- Decision 0055 connects the V2 `communication` module. A conversation opens only through a server-issued CID: `chat.channelCid` for a community, the `directGetOrCreate` operation result for a DM, the channel list or a notification for a group. `chat.status` has three values, and `syncing` renders "聊天权限同步中" rather than unavailable. A channel's LOOP-assigned prefix is the only thing that picks a surface, shared by notifications, chat search and the retained `/chat/channel/:cid` redirect.
- Chat search, forwarding and merging are Stream SDK operations with no LOOP backend. Search stays inside the channels the account can read and never enters `/v2/search`; a group or community result carries the neutral member label rather than an account-level Stream name. Forwarding caps at 20 messages, skips and counts a deleted or empty one, queries its source conversation by exact CID, and targets only one bounded page of already-joined channels, which the page states. Decision 0056 exports the merged transcript by capturing the rendered anonymous card, encoding it to PNG on device and handing it to the system share sheet through `share_plus` 12.0.2; nothing is uploaded and nothing is kept.
- Stream user tokens are minted through `POST /v2/chat/token` and `POST /v2/video/token` with the contract headers and a per-attempt idempotency key. Decision 0045's budget is unchanged: one 401 refresh and one bootstrap recovery, no caching and no persistence.
- `group-info` shows member management, the group profile and notification preferences as unavailable, and leaves a group only through the LOOP backend, never Stream's own `leave`. `community-ai` restores its layout with every functional area closed by the server's `COMMUNITY_AI_RUNTIME_DEFERRED`.
- `dm` opens only after a friendship exists. Without one it offers exactly one next step, `POST /v2/message-requests`, and states that an unreachable target answers the same way as a non-existent one, so nothing about the other account is inferred. No surface claims end-to-end encryption.
- Audio Room uses only Stream's `audio_room` type. The backend must pre-create the room, add the server-derived Stream user and assign a role without `create-call`; the app never calls `getOrCreate`. This server permission is mandatory because Stream Video 1.4.3 internally sends `create: true` during `join()`.
- A first join disables camera, microphone and screen share, while foreground playback uses the speaker route. `Joined` is presented as media connecting; only official `Connected` state is Live and eligible for Speak.
- The first Audio Room release owns microphone-only native configuration and remains foreground-only. Every terminal cleanup, including paused/hidden/detached, immediately starts native suspension, terminal mute, and one single-flight leave without letting a possibly stuck Speak/Mute/native future delay leave. Cleanup succeeds only after the retired object disappears from Stream's `activeCalls`, earlier microphone work settles, and a second post-command mute runs; fast resume and rejoin remain blocked until then, and a completed failure can only be retried. Version 1.4.3 permits one Speak start per Call, so Mute requires leave/rejoin before another Speak and the unsafe stopped-track recreation path is never entered. The flow never uses Stream's track auto-restore path. The auto-registering Stream Video Push plugin is not linked; optional Android incoming-call/background/Telecom/push components are also removed from the merged app manifest, and iOS has no camera, background, PushKit, CallKit or push entitlement configuration.
- The current Flutter code contains a shared LOOP identity Bootstrap source, strict Chat/Video user-token loaders, and a production Audio Room join path, but no production room locator. The token loader caches no Stream token, allows at most one 401 refresh and one bounded bootstrap recovery, and never makes provider connectivity a local fact. It does not prove that chat writes, presence, push, ringing, media, or voice rooms are live.
- A provider-neutral `notification.v1` classifier and root coordinator now fix the future navigation contract without enabling Firebase. Production still uses a disabled EventSource. The coordinator derives identity only from the real session plus verified bootstrap Stream user, retains at most the latest interaction for a bounded restore window, and revalidates before resolving an official Chat CID, the Audio Room lobby, or the truthful notification center. Foreground/background delivery never navigates. This does not parse raw Stream/Firebase payloads, register devices, initialize providers, ring, or claim delivery.
- Notification interaction deduplication is bounded and process-local. Cross-isolate/restart delivery and ordinary-push/VoIP ringing remain disabled until a persistent atomic receipt design, exact provider names, real payload fixtures, and native routing policy are approved.
- A persistent group with 200,000 members is not a verified Stream capability. LOOP must obtain written provider confirmation covering membership, message semantics, reactions, mentions, history, search and moderation before promising it.
- Without that written confirmation, the product must adopt a sharded-group or channel model and update the information architecture before implementation.

## Identity and safety language

- An internal immutable `user id` is the primary identity for accounts, social relationships and communication-provider mapping.
- Privy remains the only identity source. Email OTP, Google OAuth and iOS-only Apple OAuth are login methods; an external EVM wallet may use Privy SIWE to log in while signed out or link to the exact current fully verified principal. OAuth linking is not part of this slice.
- Client-visible provider configuration is supplied only through the centralized `AppConfig` plus build-profile JSON. Debug/Profile and Release declarations must match their Flutter mode; a mismatch gates every provider-backed capability. This profile is not a Production/Mainnet switch. Privy/Stream/OAuth secrets, Apple/APNs `.p8`, Sentry auth tokens, AppsFlyer server secrets, Firebase service accounts, wallet private keys, provider tokens and refresh tokens remain server/provider-owned and absent from Flutter.
- Reown is constrained to canonical `eip155` accounts and the exact Privy-generated message sent through `personal_sign`. AppKit auth, Email, Social, Embedded Wallet, SIWE, analytics, Link Mode, Solana and transaction authority are disabled, and the connection is disposed after each attempt.
- External EVM addresses are listed only as linked Privy sign-in credentials. They do not enter `PrivyWalletSummary`, `WalletSigningGateway`, balances, assets, transaction review or spot execution; the current Privy Embedded Ethereum wallet remains the sole mobile wallet-identity candidate for future backend-authorized trading.
- Signed-out SIWE selects login; an already authenticated principal selects link and may not fall back to login. Link completion is principal-bound, and LOOP does not automatically transfer, unlink, delete or merge another user's credential.
- Privy OAuth callbacks use `com.cywd.loop.privy`; Reown wallet returns use `com.cywd.loop.wallet`. Android routes them to separate activity owners, and Apple OAuth remains iOS-only behind the Sign in with Apple entitlement.
- The native client now has a principal-bound adapter for the implemented `POST /v1/bootstrap` boundary. It sends one current Privy Bearer access token, accepts only the backend-derived LOOP and Stream identities, retries one HTTP 401 at most once with a newly requested token, and keeps the validated identity in memory only.
- Each newly accepted fully verified Privy login may eagerly start that bootstrap once for its principal when a safe backend origin is configured. The request is non-blocking and its absence or failure does not reject authentication; a same-principal credential link is not another login. This is the narrow trigger refinement accepted by decision 0043.
- Production Dio construction uses two exact-origin profiles rather than one universal client. Public Hyperliquid Testnet reads use one credential-free HTTPS client that rejects Authorization and redirects before dispatch. The LOOP backend uses its own HTTPS-or-loopback-Development client; its Privy Bearer remains request-local. Both profiles reject Cookie, Proxy-Authorization, and `X-Api-Key` request headers, and neither logs payloads or credentials, persists cookies, or automatically retries requests.
- The credential-free public profile is not a provider permission surface. Current Hyperliquid mobile adapters remain Testnet `/info`-only and never call `/exchange`; the backend profile also rejects Authorization stored in Dio defaults so a Privy Bearer cannot persist beyond the owning request.
- Chat and Video share that trusted `stream_user_id` projection and now load their distinct short-lived user tokens from the LOOP backend through one principal-bound, non-persisting loader. One 401 refresh and one bootstrap recovery are independently bounded; key/user/expiry/no-store drift fails closed. Actual provider connection, refresh, channels, messages and Video behavior remain device-unverified, while Audio Room still lacks a production room locator.
- Wallet addresses are bindable and replaceable credentials. They are not database primary keys, public chat identities or communication user IDs.
- Wallet readiness is mounted directly from the current verified Privy session. The app may create the first Embedded Ethereum wallet and display/copy only a complete address, while malformed, Preview, or unverified values fail closed. Address presence never enables balances, deposits, QR funding instructions, Send, Swap, signing, or transaction claims; Manage wallets contains no fixture identities.
- Wallet prototype routes never fill missing context with ETH, a fixture wallet, or a generated signing intent. Asset details consume one typed labelled fixture, Signing Review requires an originating intent object, and the disabled DApp layout can project only the current verified Privy wallet identity. Catalog descriptions report Preview and unavailable capabilities rather than product priority as delivery evidence.
- Providerless Wallet drafts use exact typed state. Transfer amount syntax mirrors only the published positive-decimal String envelope and preserves the accepted source unchanged; it is not balance, precision, recipient, fee, or canonical-intent evidence. Every Swap projection consumes one immutable labelled snapshot, which any edit invalidates until an explicit atomic restore. Local revisions, quote layouts, and reviews never enable HTTP, idempotent transfer submission, wallet handoff, or success claims.
- Providerless Wallet controls now have observable local semantics: History and testnet filters change only labelled Preview rows, revocation is disabled, and Bridge progress consumes a typed immutable Preview snapshot. Naked Bridge progress routes fail closed, while result-state examples continue to say that no transaction was submitted. None of these controls proves wallet network support or provider activity.
- LOOP does not present an AI Guard brand or a proprietary risk score.
- Safety UI may show concrete, attributable facts such as simulation asset changes, allowance scope, malicious-domain signals, policy state and source time. Missing or stale evidence fails closed.

## System status truth

- I1 distinguishes complete device network loss, public market-data failure and private LOOP service interruption only after the composition root supplies one explicit observed scope.
- A naked `/system/offline` route renders connectivity as unknown. It does not default to offline, show retry/offline-mode actions or imply that any provider was queried.
- The reusable connectivity banner also requires an explicit scope and is not mounted globally until an approved source exists. This phase adds no connectivity plugin, health polling, automatic retry or persistence.
- I2 requires one explicit `LoopServiceErrorObservation` owned by the feature whose exact request returned an error or unconfirmed outcome. The screen remains outcome-neutral and never turns a timeout into confirmed success or failure. A naked `/system/error` route reports the error source as unavailable and never manufactures an error, support reference, retry or support action.
- The I2 projection currently carries no support reference because no exact source or grammar has been reviewed; raw exceptions, identifiers and provider payloads never enter the global surface. Retry and support are separate exact callbacks; the old generic navigation-to-Home behavior is not a retry or a support connection and cannot authorize either label.
- I3 requires one explicit `LoopForceUpdateRequirement` from a future app-level minimum-build policy gate. A naked `/system/update` route reports version policy as unavailable, remains dismissible and never claims that the build is unsafe, unsupported or unskippable.
- An explicit I3 requirement is blocking, but `Update now` appears only with its dedicated reviewed action. Because the marker carries no reason or target version, the UI asks only for a supported build and never invents a security defect or an unspecified latest version. The unused force-update dialog requires the same marker. No production requirement source, integer build comparison, policy freshness check, pre-auth root gate or verified App Store/Play Store destination is connected in this slice.
- I4 requires one explicit `LoopMaintenanceNotice` from a future app-level notice source. A naked `/system/maintenance` route reports maintenance status as unavailable and never invents the former `01:00–01:30 UTC` window, an active outage or affected services.
- The current I4 marker proves only that an approved notice is active; it carries no timing or affected-service projection. `Check again` and `View service status` require separate dedicated callbacks, while generic return-to-Home actions cannot authorize either label. No production notice source, freshness/expiry check, countdown, status destination or feature-disable policy is connected.
- I5 requires one explicit `LoopFeatureAvailabilityRestriction` from a future app-level eligibility authority. A naked `/system/region` route reports availability status as unknown and never claims that LOOP observed a location, evaluated account attributes or region-blocked a capability.
- The current I5 marker proves only that an approved current decision limits some feature access; it carries no location, reason, policy identity, affected-capability list or statement about what remains available. `Continue to LOOP` and `View eligibility policy` require separate dedicated callbacks. The backend integration catalog still marks `regional_gate` as `PENDING`, and no production policy route or reviewed destination is connected.
- I6 requires one explicit `LoopPermissionPrompt` from the feature that owns the user action. A naked `/system/permission` route reports permission context as unavailable and cannot request access, open settings or infer an OS status. The current catalog scope is Camera, Notifications and Microphone; legacy generic Biometrics copy is removed and requires a separate account/MFA capability decision.
- I6 `education` and `settingsRecovery` are separate presentation modes. Request, open-settings and not-now use three dedicated callbacks; a callback for one mode cannot authorize the other. No direct permission dependency or general platform adapter is added. Camera and Notifications remain unavailable for production composition, while Audio Room keeps its existing microphone-only native declarations and deliberate Speak-owned Stream capture path.
- I7 requires one explicit presentation-safe `LoopGlobalFeedback` from the exact feature that observed a current outcome. A naked `/preview/toast` route reports feedback context as unavailable and never renders the former fixture claims for a saved preference, delayed market data or unconfirmed order. Success requires a confirmed committed outcome; ambiguous writes remain outcome-neutral under I2 rather than being relabelled success or error.
- I7 keeps success, warning and error semantics, icon-plus-text presentation and a live-region announcement. Its optional action requires both the feedback's exact label and a dedicated callback; Dismiss uses a separate callback, while generic system callbacks cannot authorize either. This slice adds no global event bus, queue, automatic timeout, persistence or root overlay host and does not replace I1's separately sourced connectivity banner.
- I8 requires one explicit `LoopLoadingPresentation` from the exact feature whose own controller is currently pending. A naked `/preview/loading` route reports loading context as unavailable and never renders the former three-skeleton gallery or promises that content is on the way. One presentation selects exactly one list, detail or chart skeleton; list placeholder density is a visual value bounded to 1–8 at runtime, not an expected result count.
- I8 preserves one kind-specific live region and excludes every decorative block from semantics. Missing and invalid presentations fail closed. The skeleton proves no request, content identity, provider fact, progress, ETA, freshness or success. This slice adds no shimmer, timer, polling, event bus, persistence or global loading overlay, and existing Market, Chat, Profile, Watchlist, Privy, Wallet and Audio Room loading paths remain under their own reviewed state owners.

## Delivery truth

- Preview and memory adapters are UI evidence only. Their simulated writes and voice controls must never be presented as connected provider activity.
- Home Global Search has one bounded providerless Preview projection for ETH, the exact registered group, and the exact registered direct-message person. It performs case-insensitive, whitespace-normalized local filtering with a truthful no-match state. Production renders the search source as unavailable and never exposes those fixtures; the ETH example opens `/market` without inventing a provider market identity.
- Home Security Activity has no approved production event source or schema. Production therefore renders one unavailable state and no MFA, device, approval, count, severity, or all-clear claim. Its explicit Preview may demonstrate a labelled facts layout, but it has no risk score, provider request, revoke/block action, event detail, or account authority.
- A11 and H5 distinguish capability availability from enrollment and stored protection state. A11 has no providerless Switch, Save, enrollment, or Secure Storage-backed PIN claim; its only continuation explicitly makes no change. H5 has no capability-derived score, ready state, recovery conclusion, or sign-in fact. Wallet MFA and App lock stay disabled until typed setup adapters exist, while Devices and recovery information routes retain their existing fail-closed surfaces. Secure Storage remains deferred to an account-bound credential-lifecycle decision and raw PIN storage is forbidden.
- H12 persists only Reduce motion as one installation-scoped, non-sensitive Boolean through `SharedPreferencesAsync`. Both application roots restore it before mounting `LoopApp`; serialized writes preserve the latest choice, and a failure is reported as run-local rather than saved. Account changes do not clear it, a stricter system setting still wins, and Language, Display currency, and Theme remain disabled. Profile, Privacy, Notification Preferences, wallets, identities, tokens, PINs, protection state, and provider facts never enter this store.
- Home Portfolio and Net Worth have no approved production balance, allocation, or cross-product activity source. Production therefore renders those facts as unavailable and may project only the current verified Privy wallet-identity availability; wallet identity is not balance evidence. Static totals, gains, charts, allocations, Watchlist movement, unread counts, alerts, approvals, activity times, and named Preview communication targets remain inside exact visibly labelled Development Preview. This slice adds no portfolio request, refresh, retry, polling, persistence, or inferred loading/empty/error state.
- C10 New Pairs has no approved listing-time source. The public Spot snapshot cannot establish that a pair is new. Client receipt time, first local observation, volume, and canonical status are not substitutes for listing time, pool age, liquidity, ownership, or safety. Production and cached-unverified sessions remain neutrally unavailable and issue no C10 Market/Candle request; only the exact visibly labelled Development Preview may show static pair and fixture-age examples, whose sole navigation target is the bare public `/market` ledger.
- Production status requires configured SDKs, short-lived server-issued tokens, testnet or sandbox evidence, native-device verification and observable provider responses.
- The identity/Reown code and native callback declarations are implementation evidence only. The new dependency graph passed Android/iOS Debug and Release no-codesign compilation on 2026-08-29. Email OTP, Google, Apple, wallet-app return, Privy SIWE and live post-login bootstrap remain unverified on physical devices/provider dashboards; compilation does not change that status.
- The production BFF remains responsible for server-only credentials, token issuance, stable error mapping, rate limits, audit events and request correlation.
- Decision 0016 is enforced at every production entry point: the application root does not compose a Perp private gateway, all twelve retained `/perp*` paths redirect to `/market`, and catalog surfaces D1-D12 are non-interactive implementation history marked `Out of scope`. Providerless Preview assets navigate to the live Spot ledger instead of inventing a provider index; only an admitted row from the current public Spot snapshot may construct `/market/token?spotIndex=...`. A missing index fails closed to `/market`, so production cannot substitute the legacy fixture detail.
- Decisions 0013 and 0014 remain implementation history, while decision 0016 keeps their Perp account and position surfaces unmounted from product navigation. Their principal-bound, strict-expiry and Decimal-safe code remains disabled and receives no further feature work. Decision 0017 connects Market to Hyperliquid Testnet's public `spotMetaAndAssetCtxs` info response through a separate read-only adapter: tokens are joined by sparse token index, asset contexts are joined to universe rows by exact provider `coin`, and every price and volume remains an exact wire String plus `Decimal`. Decision 0019 permits a second read-only `candleSnapshot` request only for an accepted market's exact provider coin. Its case-sensitive periods are `1h`, `4h`, `1d`, `1w`, and `1M`; their accepted fixed row durations are 1h, 4h, 1d, 7d, and 30d with `T - t = duration - 1ms`. Each window and retained result is bounded to approximately/latest 120 rows, exact OHLCV remains String plus Decimal, and empty/gapped/forming history is presented truthfully without polling, auto-retry, backfill, or Preview substitution. Decision 0036 mounts that same accepted candle projection on C3 through one canonical `spotIndex`: C2 is the only in-product constructor, C3 re-admits the index from the current public market snapshot, invalid or stale identity causes zero candle requests, and symbol/navigation-extra/default-ETH fallbacks plus simulated indicators are removed. Spot account, balance, order, cancellation, execution, signing, transfer, withdrawal, and every other private path remain unavailable and backend-owned. Feature modules still own no Dio or backend route literals, and `main_preview.dart` remains the only labelled fixture composition.
- Watchlist is the first providerless personalization slice. Flutter models only the owner's versioned, grouped, ordered asset references and optimistic-conflict behavior; asset keys do not carry price, freshness, tradability, alert, or risk facts. Production stays unavailable until an authenticated integration adapter exists, while the explicit Preview root may inject a labelled in-memory implementation.
- Profile presentation remains nullable Alias plus nullable opaque avatar reference with optimistic versioning; Bio is absent and visibility remains a separate Privacy concern. Production now uses the reviewed authenticated owner-scoped adapter and never claims a save without an advanced matching resource, while the explicit Preview root may exercise labelled in-memory edits, retries and conflicts. Avatar selection stays disabled until a reviewed reference source exists.
- Public Privacy remains discoverability plus a private/followers/public copy-trade visibility preference with optimistic versioning. The old portfolio/group/activity/position controls and copy-permission form are not contract-backed and are removed. Visibility never proves discovery, follower membership, wallet access or copy execution; production now uses its reviewed authenticated owner-scoped adapter while labelled Preview remains in-memory.
- Social Privacy is a separate authenticated versioned resource with exact friend-request, group-invite and direct-message policies. Version-zero is fail-closed, replacement is complete/CAS-bound, and the settings neither prove that a relationship/Stream channel exists nor alter public discovery or copy-trade visibility.
- Notification preferences are the fourth providerless personalization slice. The exact resource contains only four Boolean owner intents—price-alert triggered, provider-activity projected, security notice, and support update—with complete optimistic replacement. Delivery remains `unavailable` for every value combination. The old six local categories, asserted operating-system state, no-op device-settings action, and Quiet hours are removed; production defaults unavailable while labelled Preview exercises only in-memory application behavior. Price Alert definition and provider/device delivery are separate future slices.
- Decision 0057 (step 5) connects the V2 `chain`, `wallet`, `market`, `watchlist` and `notifications` modules and rebuilds the nine Market pages, the seven read-only Wallet pages and `notif-settings` on the frozen contract. Every market value arrives as the fixed `{value, source, fetchedAt, ttlSeconds, quality, reasonCode}` object and is rendered with its source and observation time; `stale`, `derived` and `proxied` carry a visible marker, and an `unavailable` value renders the server's `reasonCode` rather than `0` or an em dash. A provider outage is a `200` with an unavailable block, not an HTTP error. Balance rows are a discriminated union so a failed chain read never reads as an empty wallet, the five balance meanings are shown separately and never derived from one another, the gas reserve comes from the server's `gasReservePolicy`, and net worth carries `isSpendable: false` with a `partial` count so it can never be presented as a spendable total. Only the canonical CAIP `assetId` addresses an asset and only the opaque `walletId` addresses a wallet; the Swap entry point is gated on `capability.swappable` alone. `SpotMarketRoute`, the mounted Hyperliquid Spot Market slice, the Preview Net Worth / wallet-asset / wallet-history slices, the Preview Watchlist adapter and the V1 four-intent notification-preference module are retired; the Hyperliquid adapters remain as unmounted history. `notif-settings` becomes the ten V2 categories with `security.event` locked on, delivery stays `PUSH_RUNTIME_DEFERRED` for every value combination, and a triggered price alert opens the token page through the server's own `contextRoute` and `contextParams`. The receive QR is encoded on-device by a dependency-free byte-mode encoder, so no package was added to the locked stack.
- Decision 0060 (step 8) connects the V2 `security`, `settings` and `support` modules and the public `GET /v2/meta/about`, and rebuilds `security`, `devices`, `key-export`, `social-recovery`, `settings`, `about` and `support` plus the four deferred D21 surfaces. Revoking another device is an audit projection: LOOP refuses later requests carrying that `X-Loop-Session-ID` and writes one `security.event`, while the other device's Privy access token is untouched (`effect: auditOnly`, `providerAccessTerminated: false`), so the UI says the revoke was recorded and never that the device was signed out. Revoking the current session and signing every device out stay `AUTH_STEP_UP_REQUIRED`; local sign-out remains `POST /v2/session/logout`. The six Privy security methods are permanently unavailable with their own `PRIVY_<X>_EVIDENCE_PENDING` reason, so no protection score, enabled badge, masked key or guardian list is rendered, and a device row shows only platform, client version and the bootstrap observation time — never a device name or a location. Account settings are fixed server constants shown read-only, `reduceMotion` and `theme` stay device-local per `policy.localOnly`, and the prototype's data-usage figure is absent because it has no source. `about` pairs the server's contract version, rule snapshots, terms version slot and version-less open-source register with the locally read client build. `pay`, `bridge` and `bridge-status` are entry points carrying the server's deferred reason with all three bridge steps pending and no source; `dapp` reviews a URL entirely on device — HTTPS enforced, IDN homographs named, nothing fetched so no redirect can be followed — with connect, sign and domain reputation unavailable. The Bridge Preview snapshot and the Preview DApp browser are retired.
- Decision 0062 (step 9) adopts the Launch chain slot from loop-api decision 0038: LOOP has exactly two named chain slots, `primary` (`eip155:56`, unchanged and unmovable) and `launch` (`eip155:56` by default, `eip155:97` while the Launch contract lives on the BSC testnet), declared once in `lib/core/chain/loop_chain_ids.dart`. The three fields that carry the slot — `chain/status.launchChain`, `wallets/{id}/balances.launchChain` and the `launch` capability's `evidence.launchChainId` — are optional and **absent** whenever the slot equals the primary chain, so a backend that has not set `LAUNCH_CHAIN_ID` parses and renders exactly as it did in S5/S7. A launch's chain is the launch record's own value; the catalogue and `loop-stake`, which have no record, read the capability. `eip155:97` earns a "BSC 测试网" badge — a statement of fact that blocks nothing and leaves the five reviewed states untouched — accompanied on the Launch surfaces and the wallet's Launch block by one dismissible explanation shared across them all; the sign sheet carries the badge alone, because a confirmation is not the place to read an explanation for the first time. Market, Watchlist, Swap, Send and approvals can never render any of it. The wallet's Launch block is one `eth_getBalance` result with its own snapshot, never added to the asset rows or the net worth, and a failed read shows the server's reason rather than `0`. `SigningIntent` now carries the server's canonical `chainId` and only `IntentKind.launchPurchase` may leave the primary chain; send, approve, revoke and swap are pinned to `eip155:56` by the transport, by `MoneyActionSigner` and by the wallet boundary. `privy_flutter` 0.10.1 exposes no chain-selection call at all — its provider accepts six signing methods and rejects `wallet_switchEthereumChain` — so the device signer refuses `privy_chain_switch_unsupported` off the primary chain, and no executable Launch intent ships in this step.
- Decision 0063 keeps the login page's promise and separates "this account owns no wallet" from "the wallet list was not read". After a verified Privy login whose `POST /v2/session/bootstrap` returned authorized, the client calls the existing idempotent single-flight `createFirstEthereumWallet` exactly once per principal through `LoopWalletProvisioningController`; no second creation path is added. A development preview, an `authenticatedUnverified` session, an account with no summary and an account that already reports a wallet are never asked. A failed creation is a wallet fact and never a login one: it cannot throw, it changes no session state, and it is recorded with the provider's own sentence for the wallet pages to render. Success invalidates the wallet directory, because `GET /v2/wallets` is the server's projection of Privy and a list read before the wallet existed is stale; `GET /v2/account/me` is not re-read, because its projection carries only `accountId` and `streamUserId` and re-issuing it would mean dropping a working bootstrap authorization. `LoopWalletDirectory.isEmpty` states the third answer, and the wallet tab and 我的钱包 now render it as its own block — an explanation plus one 创建钱包 button that shares the automatic attempt's single flight and is disabled while it runs — instead of "钱包清单尚未读取成功", which claims a read failed when it succeeded. A read list that names no active wallet gets a fourth sentence pointing at 我的钱包, and the five reviewed states keep saying the list was not read rather than that there is nothing in it. `networth`, `asset`, `receive` and `tx-history` render the same block rather than the skeleton they used to hold forever, because their read never starts without a wallet id. `lib/features/wallet/wallet_readiness.dart` and its unit test are deleted: the model projected the same question from the session alone, cannot distinguish an empty wallet list from an unread one, and lost its last consumer in S8; its verified-session gate now lives on `LoopWalletProvisioningController`, where the harness locks it.
- Decision 0066 (S13) gives the Watchlist an entrance. Before it, `token`'s star only opened `watchlist-edit`, and that page could reorder and remove but never add — so an empty Watchlist stayed empty and its own empty state pointed back at the star. The star is now the write: it reads `GET /v2/watchlist`, states membership as three answers (watched, not watched, not read yet), and toggles it with one whole-document `PUT` under `expectedVersion`. Adding appends the asset to the group keyed `default`, creating it in the same replacement and naming it 「自选」 when the owner has none — the server generates no key, it only validates `^[a-z0-9][a-z0-9_-]{0,31}$`, so the first group can only come from the client. Removing drops the `assetId` from every group and keeps the emptied group, because the owner named it. A `VERSION_CONFLICT` re-reads and re-applies the same intent exactly once against the version that came back; if that read already shows the intent satisfied the reload is the outcome and no second write is sent, and a second conflict is reported rather than retried. `PUT /v2/watchlist` rejects `Idempotency-Key` with `400 INVALID_REQUEST`, so no idempotency key is issued for this write — the version is what makes it safe to repeat. Both server limits are checked before any request and each carries its own outcome and sentence — 「自选已达 100 项」 / 「分组已达 20 个」 — because the server answers both with `VALIDATION_FAILED`, whose copy says the asset is not registered. The star reads its own `watchlist` capability and its own gateway mode, so a Market outage can never claim an asset is unwatched; it is a disabled control reading 「自选当前不可用」 on either closed answer — the capability document, or the controller's own `unavailable` phase, which is what a production build with no transport produces. Its `Semantics` carries the action as the label and the membership as `toggled`, with no toggle claim at all while the list is unread. Its accessible name is the action (「加入自选」/「移出自选」) and watched is the existing Lime token, not a new colour. A committed write invalidates the market overview, which projects the same resource. `watchlist-edit` gains 「新建分组」: one name field validated against the server's own bounds (trimmed, 1–40 code points, no control/format/surrogate/separator code points, ≤ 20 groups, no duplicate name) with a client-generated `g<n>` key, and the new group joins the draft so it travels in the next compare-and-set and 放弃修改 undoes it. The three circular empty-state sentences are replaced by ones that name a real path; `token` no longer navigates to `/market/watchlist`, which stays reachable from `market`, and the route manifest is unchanged.
- Decision 0064 (S11) closes the two cold-start resilience gaps the 2026-09-10 simulator run found on a throttled link. Session restore is now a three-state answer: `authenticated`/`authenticatedUnverified`, `signedOut`, and the new `restoreUnavailable`. Only an explicit Privy `Unauthenticated` — from `getAuthState` or from `authStateStream` — reaches the credential form; a transport failure and an unclassified failure both keep the session undecided on the branded launch frame with 「暂时无法确认登录状态」, the reason, and a single-flight 重试. privy_flutter 0.10.1 carries no error code across the platform channel (`PrivyException` has only `message`, and `convertToPrivyException` keeps `PlatformException.code` solely for the four MFA cases), so `PrivyFailureClassifier` maps the message: transport markers (`network`, `internet`, `offline`, `connection`, `connect`, `timed out`, `timeout`, `unreachable`, `unavailable`, `socket`, `hostname`, `dns`, `ssl`, `tls`, `certificate`) are tested first and win over credential markers (`unauthenticated`, `unauthorized`, `no user`, `no active session`, `session expired`, `invalid token`, `401`, `403`, …); everything else is `unknown` and is treated exactly like a transport failure. The D0 metadata observation keeps its contract untouched — the same two concurrent public reads, no Bearer/body/query/`X-Loop-*`/idempotency header, still non-blocking and still unable to gate login or routing — but a *failed* read is now re-armed by `LoopV2MetaObserver`: cold start once, then at most five retries at 1s→2s→4s→8s→16s, plus one attempt when the radio returns (`connectivity_plus` 7.3.1, already resolved transitively and now pinned as a direct dependency at the same version), one when the app resumes, and one per navigation, which is how "a module page opened without a capability document" is implemented. Every trigger is single-flight and is ignored while a read is in flight or a backoff retry is pending; a completed observation — including the `null` answer when no backend endpoint is configured — is never re-read, so only an `AsyncError` is retried and the module gates open by themselves without a restart. `LoopConnectivitySignal` reports only "no transport → some transport" and is never evidence that a service is reachable or that LOOP is offline. A restore that never answers is bounded at 12 seconds (`loopSessionRestoreWindowProvider`, injectable) and then becomes undecided with a cause-free message, because iOS's native `getAuthState` awaits readiness and reports no transport failure at all while only Android surfaces a `getAuthStateError`; the deadline never cancels the call, so a late `Authenticated` still lands, and it drops only the operation identity so 重试 issues a genuinely new `getAuthState`. A follow-up simulator run on a healthy network showed the flash again — the form at ~25s, Community a few seconds later, with a single `/v2/account/me` 200 proving the session was live the whole time — because privy_flutter publishes a *premature* `Unauthenticated` while it is still reading the stored credential (`authStateStream` is a `BehaviorSubject.seeded(NotReady())` fed by the native EventChannel, and `getAuthState` reads the same manager). Decision 0064 §5 therefore holds the **first** sign-out answer of a cold start for 4000 ms (`loopSessionUnauthenticatedGraceProvider`, injectable), showing the same branded launch frame rather than the form; an `authenticated`/`authenticatedUnverified` arriving inside the window wins and cancels it. Both shapes of that answer are held — the `Unauthenticated` snapshot and a restore failure classified as `authentication`, which is the same native event surfaced as an exception — and the held failure keeps its explanation if the window expires. The hold is offered once and only from `restoring`: a credential revoked after login, a 重试 the owner pressed (authentication failures included), and `exit()` all sign out immediately, and the undecided frame keeps its explanation. If the window expires while `getAuthState` is still in flight the session stays undecided under the 12-second deadline instead of falling to the form. The deprecated `isReady`/`awaitReady` are not used — "ready" there means only "not `NotReady`", which a premature `Unauthenticated` already satisfies. Residual risk: if Privy answers `Unauthenticated` instead of throwing or hanging when it cannot validate a cached session, the form still appears after the window; 4000 ms is a judgement rather than a measurement, so a slower correction would still flash; 冷启动网络抖动 stays device-unverified.
- Decision 0065 (S12) is presentation only: it changes no transport, no Stream request and no state machine. The official Stream Chat UI shipped as Stream's own default theme in English — a deep-blue own bubble, a blue send key, `Today`, `3:39 PM`, 「Send a message to start the conversation」 — because `loopStreamChatThemeData()` had no caller and stream_chat_flutter 10.3 no longer keeps its palette there: the palette moved into `StreamTheme`, a Material `ThemeExtension` that `StreamChat` reads off the **ambient** theme, and `MaterialApp` declared no `localizationsDelegates` at all, so `context.translations` fell through to the English `DefaultTranslations`. Both are now injected once, at the single `StreamChat` in `lib/app.dart`, so `community-chat`, `chat`, `chat-dm`, `chat-group`, the group Alias screens, `chat-search`, `chat-forward` and the voice-room surfaces inherit them without opting in. `lib/integrations/communication/stream_chat_appearance.dart` maps the frozen prototype onto Stream's fields using only `lib/core/theme` tokens — own bubble Lime over Ink text, other bubble `--card` over Chalk, the 5px tail on the sender's side against 16px elsewhere, `LoopMono.stamp` timestamps, `--tx3` author labels, the composer's Lime send key with Ink glyph and its `--card` field with a `--line` edge, `--ink` page ground, `--lime-soft` highlight, and a read receipt that is Lime once read and Muted before. Nothing new is introduced except one radius token, `LoopRadius.bubbleTailValue` (5), which is the prototype's own `.msg-txt` corner. The two colour ladders are the leverage: Stream reads `brand.shade100` as the ground of every outgoing surface and `brand.shade900` as the text on it, so writing Lime at 100 and Ink at 900 carries the pairing to the bubble, the quoted message, the reply attachment, the link card and the poll options at once — a deliberate inversion of Stream's convention, which expects a dark tint with bright text. Chinese comes from a LOOP-owned `StreamChatLocalizations` implementation (all 226 `Translations` members plus the accessibility labels) rather than the `stream_chat_localizations` package, which is not in the lockfile; **no dependency was added and `pubspec.lock` is unchanged**. The delegate answers for every locale on purpose, because switching the application locale to `zh` would resolve `MaterialLocalizations` to nothing — `flutter_localizations` is not a declared dependency and the built-in delegate supports `en` only — so the Stream widgets get Chinese while every Material widget keeps working. Three Stream call sites format time outside the translations — `DefaultStreamMessageFooter` hardcodes Jiffy's 12-hour `jm`, `StreamDateDivider` falls back to Jiffy past yesterday, and `ChannelLastMessageDate` uses Stream's `formatDate` whose today bucket is `jm` and whose weekday and `M/d/yyyy` bypass the localizations entirely — and Jiffy's locale is set by `StreamChat` from the ambient `Localizations`, so LOOP formats all three itself off one shared day-bucket function: a `HH:mm` clock installed globally as the `messageFooter` component builder, 今天/昨天/周一…周日/`M月d日`/`y年M月d日` separators passed to every `StreamMessageListView`, and the same ladder with the clock in the today slot on every `/chat` inbox row. The inbox's direct branch no longer returns Stream's cell untouched: `StreamChannelListItem.copyWith` exposes no timestamp slot and the default item is private, so the cell is recomposed from Stream's own public sub-widgets (`StreamChannelAvatar`, `StreamChannelName`, `ChannelListTileSubtitle` with its typing indicator and delivery status) with only the timestamp replaced. Two recursive scans over `lib/` lock it: every `StreamMessageListView(` must carry `builders: loopStreamMessageListViewBuilders()`, and every `ChannelLastMessageDate(` must carry a `formatter:`. The 68 accessibility labels `implements AccessibilityTranslations` rather than extending the English default, so a member added upstream fails the build instead of silently reading English to a screen reader. One deviation is recorded and accepted: Stream 10.3 paints no composer bar of its own, so the prototype's `--ink` `.composer` strip with its `--line` top edge is not reproduced — only the field is painted, `--card` on `backgroundElevation1` with its `--line` outline doing the separating. `intl` and `flutter_localizations` were left transitive because promoting them makes `pub get --enforce-lockfile` refuse to write, and neither is needed for two exact formats. The five reviewed states, the server-side membership proof before any channel mounts, the disabled attachments and voice recording, and every LOOP-owned string are untouched.
- Decision 0067 (S14) makes 我的社区 on `profile` a projection of the community home aggregate instead of a hardcoded 「社区成员关系尚未接入 · 未接入」. The 2026-09-10 simulator run showed the Community tab listing one joined community (Builders Guild, `owner`) from `GET /v2/community/home`'s `joined` block while the profile row three taps away claimed no source existed — a truthfulness rule stating a falsehood in the opposite direction. `ProfileCommunitiesRow` watches the existing `communityHomeControllerProvider`: no gateway, model, controller or transport is added, the read is the same single-flight one the Community tab performs, and the `community` capability is consulted through `communityCapabilityBlocks` first, so a module the server declared unavailable produces no request at all. Ready shows `N 个已加入` from `joined.items.length` — `N+ 个已加入` when `joined.truncated`, because a truncated aggregate makes the count a floor — with a subtitle naming at most two communities and the role the server confirmed (`Builders Guild（Owner） · Frog Holders（成员）`); a `muted` or `banned` membership reads 已禁言/已封禁 rather than as a role the server has suspended, through the new shared `communityMembershipLabel` the community home rows already needed. Zero joined communities is a server answer, not a missing one, so the row reads `0 个已加入` and points at discovery; every unready phase shows the em dash and names the phase from the same `CommunityViewPhase` vocabulary the other S3 surfaces use, never `0` and never 「未接入」. The row stays a navigation entry in every phase so a failed read never removes the way out: with memberships it opens `/community/discover?membership=joined` through the new `community-joined` profile destination id, and otherwise the public directory, which owns the full five-state block and the retry. No route is added and the 93-route manifest is unchanged; the Launch rows keep 「未接入」, which is still true for them.
- Decision 0068 (S15) gives the `voiceRooms` provider-evidence gate a way out. `evidence.status` had only `notApplicable` and `pending`, and `voiceRooms` was the only capability that ever published `pending`, so the whole-page block decision 0005 put on `voiceroom` / `voiceroom-full` could never be lifted no matter what the operator proved. `GET /v2/meta/capabilities` now also answers `confirmed`, published by `voiceRooms` alone once `STREAM_AUDIO_ROOM_USER_ROLE_EVIDENCE_REF` is configured, and carrying the one key that records it — `evidence.reference`, a 1–120 character operational string that is required with `confirmed`, absent (never `null`) with every other status, and rendered nowhere because it is an operator's audit string rather than product copy. The two halves are one fact, so parsing refuses both directions: a `confirmed` without a reference, a reference beside `pending` or `notApplicable`, an explicit `null`, an empty or over-length value, and the key on any capability other than `voiceRooms` — which also refuses `confirmed` elsewhere, since no other capability has this precondition to meet. `LoopCapabilityProjection.evidencePending` is now an exhaustive switch in which `pending` alone closes a surface, so a status added later must be classified rather than default to open. The voice room therefore drops its `voiceroom-evidence-pending` page on `confirmed` and runs the five states it was always written for, against the same locator contract decision 0055 fixed: the room must already exist on the server, it reaches the reviewed lobby as a constructor argument rather than through a scoped provider, and role, speaking rights and mute state still come only from Stream's official `CallState`. Confirmed evidence opens the gate, never a room: an `unavailable` or `deferred` `voiceRooms` still closes the page ahead of it, a missing room still renders the server's own `COMMUNITY_VOICE_ROOM_NOT_LIVE`, and no colour, type size or component is added. `/v2/security/capabilities` is untouched — its six Privy items stay permanently `unavailable` with `pending` evidence, because widening that parser would claim a device verification none of them has.
